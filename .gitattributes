# Normaliza los finales de linea: LF en el repositorio, CRLF en Windows al
# trabajar. Sin esto, cada render genera diferencias falsas en todo docs/.
* text=auto eol=lf

# Binarios: que Git no intente convertirlos
*.png binary
*.jpg binary
*.pdf binary
*.woff binary
*.woff2 binary
